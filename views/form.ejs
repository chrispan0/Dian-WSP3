<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1><%= title %></h1>
    <a href="/workouts">Back to Workouts</a>
  </header>
  <main>
    <form action="<%= workout ? '/workouts/' + workout._id : '/workouts' %>" method="POST">
      <input type="hidden" name="_method" value="<%= workout ? 'PUT' : 'POST' %>">
      <label for="date">Date:</label>
      <input type="date" name="date" value="<%= workout ? workout.date.toISOString().substring(0, 10) : '' %>" required>

      <label for="bodyPart">Body Part:</label>
      <input type="text" name="bodyPart" value="<%= workout ? workout.bodyPart : '' %>" required>

      <label for="description">Description:</label>
      <textarea name="description"><%= workout ? workout.description : '' %></textarea>

      <button type="submit"><%= workout ? 'Update Workout' : 'Add Workout' %></button>
    </form>
  </main>
  <footer>
    <p>Created by Dian</p>
  </footer>
</body>
</html>
