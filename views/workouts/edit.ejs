<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Workout</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1>Edit Workout</h1>
  </header>

  <div class="container">
    <div class="form-container">
      <form action="/workouts/<%= workout._id %>?_method=PUT" method="POST">
        <label for="name">Workout Name:</label>
        <input type="text" id="name" name="name" value="<%= workout.name %>" required>
        <br>
        <label for="duration">Duration (minutes):</label>
        <input type="number" id="duration" name="duration" value="<%= workout.duration %>" required>
        <br>
        <label for="intensity">Intensity:</label>
        <input type="text" id="intensity" name="intensity" value="<%= workout.intensity %>" required>
        <br>
        <label for="notes">Notes:</label>
        <textarea id="notes" name="notes"><%= workout.notes %></textarea>
        <br>
        <div class="form-buttons">
          <button type="submit" class="btn update-btn">Update Workout</button>
          <a href="/workouts/<%= workout._id %>" class="btn cancel-btn">Cancel</a>
        </div>
      </form>
    </div>
  </div>

  <footer>
    <p>Workout Planner © 2024</p>
  </footer>
</body>
</html>
