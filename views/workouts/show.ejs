<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= workout.name %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header>
    <h1><%= workout.name %></h1>
  </header>

  <div class="container">
    <div class="workout-details">
      <p><strong>Duration:</strong> <%= workout.duration %> minutes</p>
      <p><strong>Intensity:</strong> <%= workout.intensity %></p>
      <p><strong>Notes:</strong> <%= workout.notes %></p>

      <div class="action-buttons">
        <a href="/workouts/<%= workout._id %>/edit" class="btn edit-btn">Edit</a>
        <form action="/workouts/<%= workout._id %>?_method=DELETE" method="POST" style="display:inline;">
          <button type="submit" class="btn delete-btn">Delete</button>
        </form>
      </div>

      <a href="/workouts" class="btn back-btn">Back to Workouts List</a>
    </div>
  </div>

  <footer>
    <p>Workout Planner © 2024</p>
  </footer>
</body>
</html>
