<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Workouts</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header>
    <h1>All Workouts</h1>
  </header>

  <div class="container">
    <ul class="workout-list">
      <% workouts.forEach(workout => { %>
        <li class="workout-item">
          <a href="/workouts/<%= workout._id %>" class="workout-name"><%= workout.name %></a>
          <div class="action-buttons">
            <a href="/workouts/<%= workout._id %>/edit" class="edit-btn">Edit</a>
            <form action="/workouts/<%= workout._id %>?_method=DELETE" method="POST" style="display:inline;">
              <button type="submit" class="delete-btn">Delete</button>
            </form>
          </div>
        </li>
      <% }) %>
    </ul>

    <a href="/workouts/new" class="create-btn">Create New Workout</a>
  </div>

  <footer>
    <p>Workout Planner © 2024</p>
  </footer>
</body>
</html>
